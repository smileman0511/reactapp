import styled from "styled-components"
import { flexCenterRow } from "../../../styles/common"

const cssStyle = {}

cssStyle.component = styled.div`
  display: flex;
  ${flexCenterRow}
  gap: 16px;
`

export default cssStyle