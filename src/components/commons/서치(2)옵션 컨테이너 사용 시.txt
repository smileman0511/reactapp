// CommunityPage.jsx (최종 페이지)
import React from 'react';
import SearchbarComponent from './components/SearchbarComponent';
import CommunityCategoryContainer from './components/CommunityCategoryContainer';
import SearchOptionContainer from './components/SearchOptionContainer';

const CommunityPage = () => {
  return (
    <div>
      {/* 1. 카테고리 선택 바 */}
      <CommunityCategoryContainer />

      <div style={{ display: 'flex', justifyContent: 'space-between' }}>
        {/* 2. 검색창 */}
        <SearchbarComponent placeholder="글 제목을 검색하세요" />
        
        {/* 3. 정렬 옵션 */}
        {/* 이 안에서 Zustand의 setOrder2, setPage가 자동으로 동작합니다. */}
        <SearchOptionContainer />
      </div>

      {/* 4. 게시글 리스트 (Zustand의 order2 값을 보고 있을 컴포넌트) */}
      <PostList />
    </div>
  );
};